/* global Office */
// Unused in the tracer bullet — referenced by the manifest's FunctionFile
// resource but no ribbon command actions are currently wired to it.
Office.onReady(() => {
  // Ready hook — no-op.
});
